<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Upload Successful! - BuyMeACode</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-100 font-sans flex items-center justify-center min-h-screen">
    <div class="container mx-auto p-4 md:p-8 max-w-xl text-center">
        <div class="bg-white p-6 md:p-10 rounded-lg shadow-xl">
            <svg class="text-green-500 w-16 h-16 mx-auto mb-5" fill="none" stroke="currentColor" viewBox="0 0 24 24" xmlns="http://www.w3.org/2000/svg"><path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z"></path></svg>
            <h1 class="text-3xl font-bold text-gray-800 mb-3">Project Uploaded Successfully!</h1>
            <p class="text-gray-600 mb-6">
                Your project "<%= projectName %>" has been submitted.
            </p>
            <p class="text-gray-700 mb-2 font-semibold">Share this link with your client:</p>
            <div class="bg-gray-200 p-3 rounded-md mb-6">
                <input type="text" id="sharableLink" value="<%= sharableLink %>" readonly
                       class="w-full bg-transparent text-blue-600 font-mono focus:outline-none text-center md:text-left">
            </div>
            <button onclick="copyLink()"
                    class="bg-blue-500 hover:bg-blue-700 text-white font-bold py-2 px-4 rounded mb-6">
                Copy Link
            </button>
            <p id="copyMessage" class="text-sm text-green-600 h-4"></p>


            <p class="mt-8">
                <a href="/upload.html" class="text-blue-500 hover:text-blue-700 hover:underline">Upload Another Project</a>
                <span class="mx-2 text-gray-400">|</span>
                <a href="/" class="text-blue-500 hover:text-blue-700 hover:underline">View All Listings (Optional)</a>
            </p>
        </div>
        <footer class="text-center mt-8 text-gray-500 text-sm">
            <p>© 2024 BuyMeACode. A demo project.</p>
        </footer>
    </div>
    <script>
        function copyLink() {
            const linkInput = document.getElementById('sharableLink');
            linkInput.select();
            linkInput.setSelectionRange(0, 99999); // For mobile devices
            document.execCommand('copy');
            document.getElementById('copyMessage').textContent = 'Link copied to clipboard!';
            setTimeout(() => {
                document.getElementById('copyMessage').textContent = '';
            }, 2000);
        }
    </script>
</body>
</html>